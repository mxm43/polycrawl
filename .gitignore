__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
*.log
downloads/

# ── Sensitive / environment ────────────────────────────────────
.env
.env.local
.env.production
.env.prod
!.env.example

# 密码文件（不跟踪，避免误提交）
secrets/*.txt
secrets/*.key

# ── 运行时配置（本地修改不跟踪，仓库保留样例） ────────────
config/
